<div class="container body-content">
    <div class="row">
        {{#each articles}}
            <div class="col-md-6">
                <article>
                    <header>
                        <a href="article/details/{{this.id}}"><img src="{{this.image}}" width="560px" height="300px"/></a>
                        <h2>{{this.title}}</h2>
                    </header>
                    <p class="sidebar-box">{{this.content}}
                    </p>

                    <small class="author">
                        {{this.User.fullName}}
                    </small>
                    <small class="text-muted">
                        {{this.date}}
                    </small>
                    <footer>
                        <div class="pull-right">
                            <a class="btn btn-default btn-xs" href="/article/details/{{this.id}}">Read more &raquo;</a>
                        </div>
                    </footer>
                </article>
            </div>
        {{/each}}
    </div>
</div>
